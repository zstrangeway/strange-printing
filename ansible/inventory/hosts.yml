---
all:
  children:
    # The Proxmox host itself. Provisioning plays run here.
    proxmox:
      hosts:
        pve-01:
          ansible_host: 192.168.1.10      # CHANGE ME
          ansible_user: root

    # The per-printer LXCs. Everything else runs here.
    printers:
      hosts:
        klipper-01:
          ansible_host: 192.168.1.201     # CHANGE ME
        klipper-02:
          ansible_host: 192.168.1.202     # CHANGE ME
        klipper-03:
          ansible_host: 192.168.1.203     # CHANGE ME
      vars:
        ansible_user: root
